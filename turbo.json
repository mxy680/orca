{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "lint": {
      "outputs": []
    },
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$"],
      "outputs": ["apps/web/.next/**", "apps/web/.turbo/**"]
    },
    "dev": {
      "cache": false
    },
    "test": {}
  }
}
